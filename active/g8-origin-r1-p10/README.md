# EvoNOMOS Generation VIII ORIGIN-R1-P10

**Cross-Project Conditional-Law Reconstitution, Fresh Notification-Provider Demand Admission, Exact Pre-Demand Capability-Absence Certification, Mechanism-Family Transport & Replication-Trap Avoidance Gate**

Status: `P10_PRESEAL_PASS / FRESH_WORLD_PROMOTED / ZERO_TREATMENT_BYTES / NO_REPLICATION_CLAIM`

P10 exits the SumatraPDF local cell. The target is no longer replication of one project's metric pattern. The target is transport or failure of a conditional structural law across independent real maintenance demands.

Founding question:

> When a software system must absorb another member of an already recurring provider family, under what observable structural conditions does introducing or strengthening an abstraction reduce later propagation burden enough to repay its setup and coordination cost?

A transported law is not established by reproducing the same sign twice. A contrary sign in a new topology is equally valuable if it localizes a moderator or failure boundary.

## Fresh world

Primary candidate: `2GT-Media-Group-LLC/mikrotik-manager#169` — add Gotify as an additional alert/notification provider.

Exact conservative pre-demand source: `68f3c7fc50a91f7dee79f7af914d9dd205498fc7`.

At that source:
- Gotify is absent.
- alert-channel type is an explicit finite union: email/slack/discord/telegram/ntfy.
- dispatch is an explicit switch with dedicated sender methods.
- accepted channel types, secret masking, DB CHECK constraints, API types and UI choices repeat provider membership across surfaces.
- a shared HTTP JSON helper exists, but no generic alert-channel provider/adapter abstraction exists.

## Rival family

`DIRECT_DEDICATED`
- add Gotify as another dedicated channel kind using the existing switch/dedicated-method architecture.

`INVERT_CHANNEL_ADAPTER`
- introduce a typed channel-provider adapter boundary/registry owning provider-specific validation, sensitive-field policy and send behavior; instantiate existing providers and Gotify through that boundary without changing user-visible semantics.

No treatment bytes are created in P10. The next phase must freeze the exact functional oracle and narrow the INVERT scope before construction.

## Anti-replication doctrine

P10 does not ask whether “INVERT wins again.” It asks whether the same mechanism-level proposition survives a topology change from SumatraPDF's pre-existing polymorphic provider boundary to MikroTik Manager's explicit union/switch/channel-surface duplication.

A negative or reversed result is not a failed replication. It is evidence about the support domain of the candidate law.
